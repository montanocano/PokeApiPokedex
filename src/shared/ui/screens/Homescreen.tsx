import React from "react";
import { TamaguiTestScreen } from "./Tamaguitestscreen";

// Placeholder — replace with the real HomeScreen once the list is built.
// For now it just renders the Tamagui test screen to verify the setup.
export function HomeScreen() {
  return <TamaguiTestScreen />;
}