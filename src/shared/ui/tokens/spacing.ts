export const spacing = {
  0: 0,
  xs: 4, // 0.5x — minimum inline separation
  sm: 8, // 1x   — small internal padding
  md: 16, // 2x   — standard card/container padding
  lg: 24, // 3x   — section separation
  xl: 32, // 4x   — screen margin
  xxl: 48, // 6x   — major block separation
} as const;
